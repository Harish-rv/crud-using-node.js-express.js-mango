<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>node + sMongo crud</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <style>
        body{
            margin: 0;
            padding: 0;
            background: #c9c9c9 !important;
        }
        .wrapper{
            display: grid;
            justify-content: center;
            align-items: center;
        }
        .crudtable{
            background: white;
        }
        thead{
            background: linear-gradient(90deg, rgba(0,36,33,1) 0%, rgba(0,212,255,1) 0%, rgba(190,237,117,1) 100%);
        }
        .head{
            text-align: center;
            color: white;
            background: black;
            padding: 20px;
        }
        .subBtn{
            height: 40px;margin: auto;
        }
    </style>
</head>
<body>
    <div class="head">
        <h2>Simple Crud using Node + Express + Mongo Crud + hbs engine</h2>
    </div>
    <div class="wrapper">
        {{!-- {{message}} --}}
        <div class="form my-3">
            <form action="/create" method="post" class="d-flex">
                <input type="post" class="form-control m-1" name="name" placeholder="name"/>
                <input type="post" class="form-control m-1" name="age" placeholder="age"/>
                <input type="post" class="form-control m-1" name="department" placeholder="department"/>
                <input type="post" class="form-control m-1" name="phone" placeholder="phone"/>
                <input type="submit" class="btn btn-dark mh-10 subBtn">
            </form>
        </div>
        <table class="table table-striped crudtable">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Edit</th>
                    <th>Delete</th>
                </tr>
            </thead>
            {{#each values}}
            <tr>
                <td>{{this._id}}</td>
                <td>{{this.name}}</td>
                <td><a class="btn-dark rounded p-1 text-decoration-none small" href="edit/?edit_id={{this._id}}">EDIT</a></td>
                <td><a class="btn-dark rounded p-1 text-decoration-none small" href="delete/?delete_id={{this._id}}">Delete</a></td>
            </tr>
            {{/each}}
        </table>
    </div>
</body>
</html>